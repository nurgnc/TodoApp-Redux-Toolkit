import React from "react";

const Form = () => {
  return (
    <form>
      <input
        className="new-todo"
        autoFocus
        placeholder="What needs to be done?"
      />
    </form>
  );
};

export default Form;
